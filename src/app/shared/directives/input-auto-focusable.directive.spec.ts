import { InputAutoFocusableDirective } from './input-auto-focusable.directive';

describe('InputAutoFocusableDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAutoFocusableDirective();
    expect(directive).toBeTruthy();
  });
});
